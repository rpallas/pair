.container
    .well(ng-show="!hasUsers()") Unable to get user list
    table.table(ng-show="hasUsers()")
        tr(ng-repeat="user in users")
            td
                a(href="/browse/{{ user._id }}") {{ user.displayName }}