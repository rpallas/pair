.container
    form.form-inline(role="form" name="emailRequestForm")
        .form-group(has-success, has-feedback)
            label.control-label(for="email") Email
            input.form-control(ng-model="email" type="email" name="email" placeholder="Please enter your email address")
            span.glyphicon.glyphicon-ok.form-control-feedback
        .form-group
            .col-md-10.col-md-offset-2
                .pull-right
                    button.btn.btn-primary(ng-click="submitEmail()" ng-disabled="emailRequestForm.$invalid") Submit
                    | &nbsp;
                    a.btn.btn-default(href="/") Cancel