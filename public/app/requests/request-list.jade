.container
    h2 Pair Requests
    p Track your sent and recieved requests here
    h4 Recieved:
    table.table.table-hover.table-striped.table-condensed
        thead
            tr
                th From
                th Recieved Date
                th State
            tbody
                tr(ng-repeat="request in receivedRequests()")
                    td
                        a(href="/users/{{ request.fromUser }}") {{ request.fromUser }}
                    td {{ request.sentDateTime | date:'dd/MM/yyyy hh:mm' }}
                    td {{ request.state }}
    h4 Sent:
    table.table.table-hover.table-striped.table-condensed
        thead
            tr
                th To
                th Sent Date
                th State
            tbody
                tr(ng-repeat="request in sentRequests()")
                    td
                        a(href="/users/{{ request.toUser }}") {{ request.toUser }}
                    td {{ request.sentDateTime | date:'dd/MM/yyyy hh:mm' }}
                    td {{ request.state }}